import {FormControl, FormLabel, FormErrorMessage, FormHelperText, Input, Box, Button, Textarea,} from '@chakra-ui/react'
import { useState } from 'react';
//checkboxes of common knows about terms for REI, maybe a custom input form as well

function KnowsAboutForm() {

    return {
        KAIRender : (
            <Box>
                <p>Knows About Info form will go here</p>
                <FormControl>
                

                </FormControl>
            </Box>
        )
    }
}
export default KnowsAboutForm;